
# Optional Backend Proxy

Run this if you prefer a standalone proxy instead of Next.js API routes.

```
npm i
npm start  # runs on http://localhost:4000
```

Then set your frontend to call `http://localhost:4000/api/*`.
